#include "special_effects.h"

void Special_effects::setCameraPointer(Camera* camera) {
    this->camera_ptr = camera;
}

void Special_effects::swaying() {
//    vector<float> outVert;
//    for (int i = 0; i < outVert.size() / 3; i++) {
//        // CUBE MOVEMENT
//        outVert[i * 3] += sin(time + outVert[i * 3 + 1]) / 50;
//        time += 0.1;
//        // TEXTURE MOVEMENT
//        // outUv[i * 2] += sin(time + outVert[i * 2 + 1]) / 10
//    }
}
